# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: app-deploy
'on':
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: temurin
        cache: maven
    - name: Build and test
      run: mvn -B verify
      working-directory: app
  image:
    runs-on: ubuntu-latest
    needs:
    - build
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_aws_region: us-east-1
      TF_VAR_api_allowed_cidr: ${{ secrets.API_ALLOWED_CIDR }}
      TF_VAR_billing_alarm_email: ${{ secrets.BILLING_ALARM_EMAIL }}
    timeout-minutes: 25
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Terraform init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - name: Build, scan and push the image
      run: bash scripts/build-push-image.sh "${{ github.sha }}"
  deploy:
    runs-on: ubuntu-latest
    needs:
    - image
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_aws_region: us-east-1
      TF_VAR_api_allowed_cidr: ${{ secrets.API_ALLOWED_CIDR }}
      TF_VAR_billing_alarm_email: ${{ secrets.BILLING_ALARM_EMAIL }}
    timeout-minutes: 25
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v3
      with:
        version: v1.33.0
    - name: Terraform init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - id: tf
      name: Read cluster name and image reference from state
      run: "set -euo pipefail\nCLUSTER=\"$(bash scripts/tf-output.sh cluster_name\
        \ infra)\"\nif [ -z \"$CLUSTER\" ]; then\n  echo \"ERROR: cluster_name is\
        \ not present in terraform state.\" >&2\n  echo \"The cluster must be provisioned\
        \ before deploying the app.\" >&2\n  exit 1\nfi\necho \"::add-mask::$CLUSTER\"\
        \necho \"cluster=$CLUSTER\" >> \"$GITHUB_OUTPUT\"\n"
    - name: Deploy the application under temporary API access
      run: "set -euo pipefail\nECR_URL=\"$(bash scripts/tf-output.sh ecr_repository_url\
        \ infra)\"\nif [ -z \"$ECR_URL\" ]; then\n  echo \"ERROR: ecr_repository_url\
        \ is not present in terraform state.\" >&2\n  exit 1\nfi\necho \"::add-mask::$ECR_URL\"\
        \nbash scripts/ci-api-access.sh \\\n  \"${{ steps.tf.outputs.cluster }}\"\
        \ us-east-1 \"${{ secrets.API_ALLOWED_CIDR }}\" \\\n  -- bash scripts/deploy-app.sh\
        \ \"${ECR_URL}:${{ github.sha }}\" \"${{ github.sha }}\"\n"
  verify-app:
    runs-on: ubuntu-latest
    needs:
    - deploy
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_aws_region: us-east-1
      TF_VAR_api_allowed_cidr: ${{ secrets.API_ALLOWED_CIDR }}
      TF_VAR_billing_alarm_email: ${{ secrets.BILLING_ALARM_EMAIL }}
    timeout-minutes: 25
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v3
      with:
        version: v1.33.0
    - name: Terraform init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - id: tf
      name: Read cluster name from state
      run: "set -euo pipefail\nCLUSTER=\"$(bash scripts/tf-output.sh cluster_name\
        \ infra)\"\nif [ -z \"$CLUSTER\" ]; then\n  echo \"ERROR: cluster_name is\
        \ not present in terraform state.\" >&2\n  exit 1\nfi\necho \"::add-mask::$CLUSTER\"\
        \necho \"cluster=$CLUSTER\" >> \"$GITHUB_OUTPUT\"\n"
    - name: Verify the public endpoint under temporary API access
      run: "bash scripts/ci-api-access.sh \\\n  \"${{ steps.tf.outputs.cluster }}\"\
        \ us-east-1 \"${{ secrets.API_ALLOWED_CIDR }}\" \\\n  -- bash scripts/verify-app.sh\n"
